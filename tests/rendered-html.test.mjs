import assert from "node:assert/strict";import test from "node:test";
async function render(){const u=new URL("../dist/server/index.js",import.meta.url);u.searchParams.set("test",`${process.pid}-${Date.now()}`);const{default:worker}=await import(u.href);return worker.fetch(new Request("http://localhost/",{headers:{accept:"text/html"}}),{ASSETS:{fetch:async()=>new Response("Not found",{status:404})}},{waitUntil(){},passThroughOnException(){}})}
test("server-renders Neko Hub dashboard",async()=>{const response=await render();assert.equal(response.status,200);const html=await response.text();assert.match(html,/<title>Neko Hub<\/title>/i);assert.match(html,/Sharper signals/);assert.match(html,/LIVE COSMETIC CATALOG/);assert.match(html,/Neko AI is taking shape/);assert.match(html,/class="boot-lockup"/);assert.match(html,/<strong>NEKO HUB<\/strong>/);assert.doesNotMatch(html,/INITIALIZING FORTNITE INTELLIGENCE/);assert.match(html,/Shuffle locker/);assert.doesNotMatch(html,/codex-preview|Your site is taking shape/) });
test("renders accessible product landmarks",async()=>{const html=await(await render()).text();assert.match(html,/<aside>/);assert.match(html,/<main>/);assert.match(html,/<nav/);assert.match(html,/Search cosmetics, sets, rarities/);assert.match(html,/Log In/);assert.match(html,/About/) });
