# Authentication

Identity is server-owned. Browser sessions use secure, HTTP-only credentials. Sites-hosted identity-aware routes use dispatcher-provided authenticated user headers; authorization is enforced server-side. Roles and permissions are returned by the Identity Service, never inferred by the client.
